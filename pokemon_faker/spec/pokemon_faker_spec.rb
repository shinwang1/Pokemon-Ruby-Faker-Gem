require 'spec_helper'

describe PokemonFaker do
  it 'has a version number' do
    expect(PokemonFaker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
